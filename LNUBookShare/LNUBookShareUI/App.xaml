<Application x:Class="LNUBookShareUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LNUBookShareUI">
    <Application.Resources>

        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <SolidColorBrush x:Key="MainBackgroundBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="ControlBackgroundBrush" Color="#1E293B"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#334155"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#3B82F6"/>
        <SolidColorBrush x:Key="TextBrush" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="SubTextBrush" Color="#94A3B8"/>

        <StreamGeometry x:Key="BookIconGeometry">
            M 18 2 H 6 C 3.79 2 2 3.79 2 6 v 12 c 0 2.21 1.79 4 4 4 h 12 c 1.1 0 2 -0.9 2 -2 V 4 c 0 -1.1 -0.9 -2 -2 -2 Z M 6 4 h 12 v 8 l -3.5 -2 l -3.5 2 V 4 H 6 C 4.9 4 4 4.9 4 6 v 12 c 0 -0.55 0.45 -1 1 -1 h 9 c 0.55 0 1 0.45 1 1 s -0.45 1 -1 1 H 6 c -1.66 0 -3 -1.34 -3 -3 s 1.34 -3 3 -3 Z
        </StreamGeometry>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Height" Value="120"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                </Setter.Value>
            </Setter>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                To="1.05" Duration="0:0:0.2"/>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                To="1.05" Duration="0:0:0.2"/>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.BlurRadius)"
                                To="10" Duration="0:0:0.2"/>
                            <!--<DoubleAnimation 
                                Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.Opacity)"
                                To="0.3" Duration="0:0:0.2"/>-->
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                To="1.0" Duration="0:0:0.2"/>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                To="1.0" Duration="0:0:0.2"/>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.BlurRadius)"
                                To="0" Duration="0:0:0.2"/>
                            <!--<DoubleAnimation 
                                Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.Opacity)"
                                To="0" Duration="0:0:0.2"/>-->
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LoadingSpinnerStyle" TargetType="Path">
            <Setter Property="Stroke" Value="#3498db"/>
            <Setter Property="StrokeThickness" Value="4"/>
            <Setter Property="Data" Value="M 10,50 A 20,20 0 1 1 50,10"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever">
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                From="0"
                                To="360"
                                Duration="0:0:1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

    </Application.Resources>
</Application>