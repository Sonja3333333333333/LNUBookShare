<Application x:Class="LNUBookShareUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LNUBookShareUI"
             >
    <Application.Resources>        
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="LoadingSpinnerStyle" TargetType="Path">
            <Setter Property="Stroke" Value="#3498db"/>
            <Setter Property="StrokeThickness" Value="4"/>
            <Setter Property="Data" Value="M 10,50 A 20,20 0 1 1 50,10"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform/>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever">
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                From="0"
                                To="360"
                                Duration="0:0:1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>


    </Application.Resources>

</Application>
