<Window x:Class="LNUBookShareUI.Views.ProfileView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LNUBookShareUI.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:ProfileViewModel}"
        Title="{Binding Profile.FullName, FallbackValue='Профіль'}" 
        Height="750" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundColor}">

    <Window.Resources>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline" Cursor="Hand"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource PrimaryDarkColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionIconButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EEE"/>
                    <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteIconButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionIconButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFEEEE"/>
                    <Setter Property="BorderBrush" Value="#DC3545"/>
                    <Setter Property="Foreground" Value="#DC3545"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Height" Value="120"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                To="1.05" Duration="0:0:0.2"/>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                To="1.05" Duration="0:0:0.2"/>
                            
                            </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)"
                                To="1.0" Duration="0:0:0.2"/>
                            <DoubleAnimation 
                                Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                To="1.0" Duration="0:0:0.2"/>
                            
                            </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" 
                Content="← Назад" 
                HorizontalAlignment="Left" 
                Margin="0,0,0,15" 
                Background="Transparent"
                BorderThickness="0"
                Foreground="{StaticResource TextSecondaryColor}"
                Cursor="Hand"
                FontSize="14"
                Command="{Binding GoBackCommand}" 
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Ellipse Grid.Column="0" Width="100" Height="100" Margin="0,0,25,0" Stroke="{StaticResource BorderColor}" StrokeThickness="1">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding Profile.AvatarPath}" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Profile.FullName}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryColor}"/>
                    <TextBlock Text="{Binding Profile.Email}" FontSize="15" Margin="0,5,0,5" Foreground="{StaticResource TextSecondaryColor}"/>
                    <TextBlock Text="{Binding Profile.FacultyName}" FontSize="15" FontStyle="Italic" Foreground="{StaticResource TextSecondaryColor}"/>
                </StackPanel>

                <Button Grid.Column="2" 
                        Content="✏️ Редагувати профіль" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Background="Transparent"
                        Foreground="{StaticResource PrimaryColor}"
                        BorderBrush="{StaticResource PrimaryColor}"
                        BorderThickness="1"
                        VerticalAlignment="Top"
                        Padding="15,8"
                        Visibility="{Binding IsMyProfile, Converter={StaticResource BoolToVis}}"
                        Command="{Binding OpenEditProfileCommand}"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryColor}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="Книги користувача"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMyProfile}" Value="True">
                                <Setter Property="Text" Value="Мої книги"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Сортувати:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondaryColor}"/>
                <ComboBox ItemsSource="{Binding SortOptions}" 
                          SelectedValue="{Binding SelectedSort}"
                          DisplayMemberPath="Value"
                          SelectedValuePath="Key"
                          VerticalAlignment="Center" 
                          Padding="5"
                          MinWidth="120" Margin="0,0,20,0"/>

                <RadioButton Content="Усі" GroupName="StatusFilter" VerticalAlignment="Center" Margin="0,0,10,0"
                             Command="{Binding SetFilterAllCommand}" IsChecked="True"/>
                <RadioButton Content="Доступні" GroupName="StatusFilter" VerticalAlignment="Center" Margin="0,0,10,0"
                             Command="{Binding SetFilterAvailableCommand}"/>
                <RadioButton Content="Видані" GroupName="StatusFilter" VerticalAlignment="Center"
                             Command="{Binding SetFilterIssuedCommand}"/>
            </StackPanel>
        </Grid>

        <ListView Grid.Row="3" 
                  ItemsSource="{Binding OwnedBooksView}" 
                  HorizontalContentAlignment="Stretch"
                  BorderThickness="0"
                  Background="Transparent"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Focusable" Value="False"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,5,10" Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="60" Height="90" 
                                    Margin="0,0,15,0" 
                                    Background="#FAFAFA" 
                                    BorderBrush="{StaticResource BorderColor}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    ClipToBounds="True">
                                <Image Source="{Binding CoverPath}" Stretch="UniformToFill" />
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <Button Content="{Binding Title}" 
                                        Style="{StaticResource LinkButtonStyle}"
                                        FontSize="16" 
                                        FontWeight="Bold" 
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,5"
                                        Command="{Binding DataContext.OpenBookDetailsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding BookId}"/>

                                <TextBlock Text="{Binding Author}" FontStyle="Italic" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,5"/>

                                <TextBlock FontSize="13" Foreground="{StaticResource TextSecondaryColor}">
                                    <Run Text="Статус:" FontWeight="SemiBold"/>
                                    <Run Text="{Binding Status}" Foreground="{StaticResource TextPrimaryColor}"/>
                                    <Run Text=" | "/>
                                    <Run Text="Рік:" FontWeight="SemiBold"/>
                                    <Run Text="{Binding Year}"/>
                                </TextBlock>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"
                                        Visibility="{Binding DataContext.IsMyProfile, 
                                                     RelativeSource={RelativeSource AncestorType=Window}, 
                                                     Converter={StaticResource BoolToVis}}">

                                <Button Content="✏️" 
                                        ToolTip="Редагувати книгу"
                                        Style="{StaticResource ActionIconButtonStyle}"
                                        Margin="0,0,10,0"
                                        Command="{Binding DataContext.OpenEditBookCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding BookId}"/>

                                <Button Content="🗑️" 
                                        ToolTip="Видалити книгу"
                                        Style="{StaticResource DeleteIconButtonStyle}"
                                        Command="{Binding DataContext.DeleteBookCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding BookId}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Grid.Row="4" 
                Content="➕ Додати нову книгу" 
                Style="{StaticResource PrimaryButtonStyle}"
                HorizontalAlignment="Center" 
                Width="250"
                Height="45"
                FontSize="16"
                Margin="0,15,0,0"
                ToolTip="Додати книгу до своєї бібліотеки"
                Visibility="{Binding IsMyProfile, Converter={StaticResource BoolToVis}}"
                Command="{Binding OpenAddBookCommand}"/>

        <Grid Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"
              Panel.ZIndex="100"
              Grid.RowSpan="5">
            <Path Style="{StaticResource LoadingSpinnerStyle}"
                  Width="20"
                  Height="20"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"/>
        </Grid>

    </Grid>
</Window>