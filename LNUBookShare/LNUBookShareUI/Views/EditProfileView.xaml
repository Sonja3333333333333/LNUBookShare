<Window x:Class="LNUBookShareUI.Views.EditProfileView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        
        xmlns:vm="clr-namespace:LNUBookShareUI.ViewModels"
        
        mc:Ignorable="d" 
        d:DataContext="{d:DesignInstance Type=vm:EditProfileViewModel}"
        
        Title="Редагувати профіль" 
        Height="450" 
        Width="350"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,30">
            <Ellipse Width="100" Height="100" Stroke="Gray" StrokeThickness="2">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding ProfileImageUrl}" 
                        Stretch="UniformToFill"/>
                </Ellipse.Fill>
            </Ellipse>
            <Button Content="Додати (Змінити) фото" VerticalAlignment="Center" Margin="15,0,0,0"
                    Command="{Binding ChangePhotoCommand}"
            />
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Прізвище"/>
        <TextBox Grid.Row="1" Text="{Binding LastName}" Margin="0,20,0,10"/>

        <TextBlock Grid.Row="2" Text="Ім'я" Margin="0,10,0,0"/>
        <TextBox Grid.Row="2" Text="{Binding FirstName}" Margin="0,30,0,10"/>

        <TextBlock Grid.Row="3" Text="Факультет" Margin="0,10,0,0"/>
        <ComboBox Grid.Row="3" Margin="0,30,0,10"
                  ItemsSource="{Binding Faculties}"
                  SelectedItem="{Binding SelectedFaculty}"
                  DisplayMemberPath="Name" />
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Скасувати" Width="100" Margin="0,0,10,0" IsCancel="True"
                    Command="{Binding CancelCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />

            <Button Content="Зберегти" Width="100" IsDefault="True"
                    Command="{Binding SaveCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
        </StackPanel>
    </Grid>
</Window>