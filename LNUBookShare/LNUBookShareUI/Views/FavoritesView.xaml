<Window x:Class="LNUBookShareUI.Views.FavoritesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             
             xmlns:vm="clr-namespace:LNUBookShareUI.ViewModels"
             
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:FavoritesViewModel}">

	<StackPanel Margin="10">
		<StackPanel Orientation="Horizontal" Margin="0,0,0,15">
			<Button Content="Усі" Command="{Binding ApplyFilterCommand}" CommandParameter="All" Margin="0,0,10,0"/>
			<Button Content="Доступні" Command="{Binding ApplyFilterCommand}" CommandParameter="Available" Margin="0,0,10,0"/>
			<Button Content="Видані" Command="{Binding ApplyFilterCommand}" CommandParameter="Issued"/>
		</StackPanel>

		<ListView ItemsSource="{Binding FavoriteBooks}" HorizontalContentAlignment="Stretch">
			<ListView.ItemTemplate>
				<DataTemplate>
					<Grid Margin="0,5">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>

						<StackPanel Grid.Column="0">
							<TextBlock Text="{Binding Title}" FontWeight="Bold" />
							<TextBlock Text="{Binding Author}" />
						</StackPanel>

						<Button Grid.Column="1"
                                Content="Видалити"
                                HorizontalAlignment="Right"
                                Command="{Binding DataContext.RemoveFromFavoritesCommand, 
                                          RelativeSource={RelativeSource AncestorType={x:Type ListView}}}"
                                
                                CommandParameter="{Binding BookId}" />
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

	</StackPanel>
</Window>