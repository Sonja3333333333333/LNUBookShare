<Window x:Class="LNUBookShareUI.Views.BookDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             
             xmlns:vm="clr-namespace:LNUBookShareUI.ViewModels"
             
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:BookDetailsViewModel}">

	<StackPanel Margin="20">
		<Image Source="{Binding Book.CoverPath}" Height="250" Stretch="Fill" />

		<TextBlock Text="{Binding Book.Title}" FontSize="24" FontWeight="Bold" Margin="0,10,0,0"/>
		<TextBlock Text="{Binding Book.Author}" FontSize="18" FontStyle="Italic" />

		<TextBlock Text="{Binding Book.OwnerFullName, StringFormat='Власник: {0}'}" Margin="0,10,0,0" />

		<Button Command="{Binding ToggleFavoriteCommand}" Content="Вподобати / Видалити" Margin="0,20,0,0" Padding="10,5"/>
	</StackPanel>
</Window>